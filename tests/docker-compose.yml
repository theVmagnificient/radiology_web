version: '3'


services:
  tests:
    build: .
    environment:
      KAFKA_BROKER_URL: broker:9092

    volumes:
      - aimed_archives:/mnt/archives
      - aimed_results:/mnt/results
        # command: ["/bin/bash", "-c", " while :; do echo 'Hit CTRL+C'; sleep 1; done"]
    command: ["/bin/bash", "-c", "sleep 100 && cd /app/code && py.test -l -v dnn_test.py --junit-xml=dnn_tests.xml"]
    #consumer:
    #build: ./producer
    #environment:
    #   KAFKA_BROKER_URL: broker:9092
    #   TRANSACTIONS_TOPIC: dnn.results


networks:
  default:
    external:
      name: kafka-network 

volumes:
  aimed_archives:
    external: true
  aimed_results:
    external: true
